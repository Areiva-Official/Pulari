{
  "name": "pulari-lambda",
  "version": "1.0.0",
  "description": "Pulari Restaurant — AWS Lambda backend (API Gateway HTTP API → Lambda → DynamoDB)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.620.0",
    "@aws-sdk/client-sesv2": "^3.1075.0",
    "@aws-sdk/lib-dynamodb": "^3.620.0",
    "aws-jwt-verify": "^4.0.1",
    "stripe": "^16.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^22.5.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.5.3"
  }
}
